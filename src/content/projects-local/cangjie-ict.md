---
title: 仓颉 Agent Harness / CLI 优化
---

# 仓颉 Agent Harness / CLI 优化

私有仓库 `Logikinet/cangjie-ict`。

仓颉 Agent Harness 与 CLI 工程优化相关实践。配置 `PROJECTS_READ_TOKEN` 且在 `project-repositories.ts` 将 `readmePublicSafe` 设为 `true` 后，可同步 README 到站点。
